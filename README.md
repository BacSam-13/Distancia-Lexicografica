This is a basic code that develops the concept of a lexicographic distance between two strings.

We implement a matrix that define the costo of different opperations; such as add, delete and replace letters.
